name: fhir_test

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  collection: ^1.18.0
  fhir:
    path: ../fhir
  # fhir: ^0.11.3
  fhir_bulk:
    path: ../fhir_bulk
  # fhir_bulk: ^0.11.2
  http: ^1.2.1

dev_dependencies:
  test: ^1.25.2
